---
title: "CLI"
description: "Ad-hoc calls and scripting against the rial API."
---

For ad-hoc calls and scripting, the `rial` CLI wraps the same public API.

<CodeGroup>
```bash install
npm i -g @rial/cli
```
</CodeGroup>

```bash
rial --help
```
